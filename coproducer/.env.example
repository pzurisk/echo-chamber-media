# Copy to coproducer/.env and fill in. The real .env is gitignored, keep it that way.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# WebSocket port the jweb UI connects to. Leave as is unless 7400 is taken.
COPRODUCER_WS_PORT=7400
